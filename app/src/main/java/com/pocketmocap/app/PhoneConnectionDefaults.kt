package com.pocketmocap.app

/**
 * Connection defaults for the phone capture node.
 *
 * A phone must not ship with a developer LAN address. The operator-provided
 * Pocap PC link, QR code, or manually typed server URL is the source of truth.
 */
object PhoneConnectionDefaults {
    const val INITIAL_SERVER_URL = ""
}
